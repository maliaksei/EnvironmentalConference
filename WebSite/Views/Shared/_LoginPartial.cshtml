@using Microsoft.AspNet.Identity
@using WebSite.Properties
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @*@Html.AntiForgeryToken()*@

    <ul class="nav navbar-nav navbar-right">
        <li>
            @*@Html.ActionLink(Resources.HelloOneText + " " + User.Identity.GetUserName() + "!", "ManagePassword", "Account", routeValues: null, htmlAttributes: new { title = "Manage" })*@
            @Html.ActionLink(Resources.HelloOneText + " " + User.Identity.GetUserName() + "!", "PersonalUserAccount", "UserManage", routeValues: null, htmlAttributes: new { title = "Manage" })
        </li>
        <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
    </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink(Resources.RegisterBarName, "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink(Resources.LogInBarName, "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}
